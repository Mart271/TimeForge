/**
 * @deprecated Moved to `modules/storage`. Use `StorageService` / `UploadService`
 * from the provider-swappable StorageModule (local | supabase). This re-export
 * remains only for backward compatibility.
 */
export { StorageService } from '../modules/storage/storage.service';
